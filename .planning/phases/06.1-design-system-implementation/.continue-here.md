---
phase: 06.1-design-system-implementation
task: 6
total_tasks: 7
status: in_progress
last_updated: 2026-03-25T13:21:01.941Z
---

<current_state>
Working on design system enhancements for MyCareer app. Two parallel efforts:
1. **LiquidGlassSidebar** - Fully integrated into AppLayout, replaces old Sidebar component
2. **Audio-reactive visualizer** - Hook created, needs wiring to InterviewScreen

Currently at the integration step: need to pass AudioRecorder's analyser to the visualizer.
</current_state>

<completed_work>

- Task 1: Create LiquidGlassSidebar component - Done
  - `components/ui/liquid-glass/lg-sidebar.tsx` - Full liquid glass styling
  - `components/ui/liquid-glass/lg-sidebar-demo.tsx` - Demo usage examples

- Task 2: Add disabled item support to NavigationItem - Done
  - Added `disabled?: boolean` to NavigationItem interface
  - Styled disabled items with opacity and pointer-events

- Task 3: Integrate LiquidGlassSidebar into AppLayout - Done
  - Updated `components/AppLayout.tsx` to use LiquidGlassSidebar directly
  - Removed old sidebar slot pattern and mobile menu handling

- Task 4: Update MyCareerApp to use new AppLayout interface - Done
  - Removed Sidebar import and mobileMenuOpen state
  - Simplified to pass `currentStep`, `onNavigate`, `disabledSteps` directly

- Task 5: Create useAudioAnalyser hook - Done
  - `hooks/use-audio-analyser.ts` - Analyzes frequency data from AnalyserNode
  - Returns volume, bass, mid, treble levels (0-1)

- Task 6: Update useAgentAudioVisualizerAura for audio reactivity - In progress
  - Added optional `analyser` parameter
  - Audio-reactive when state is 'speaking' or 'listening'
  - Modulates scale, amplitude, frequency, brightness based on audio
</completed_work>

<remaining_work>

- Task 6: Integrate analyser from AudioRecorder into InterviewScreen
- Task 7: Test audio-reactive visualizer in interview flow
</remaining_work>

<decisions_made>

- Decided to have LiquidGlassSidebar manage its own mobile state because the component is self-contained with hamburger menu, overlay, and collapse logic - no need for external mobileMenuOpen state
- Chose optional analyser parameter approach over creating audio context in the hook because it allows reuse with different audio sources (AudioRecorder, microphone, etc.)
</decisions_made>

<blockers>
None
</blockers>

<context>
The liquid-glass sidebar now matches the design system with backdrop-blur, multi-layer shadows, and gradient overlays. The audio visualizer is ready to be reactive - just need to wire up the AnalyserNode from AudioRecorder.getAnalyser() in InterviewScreen and pass it to the AgentAudioVisualizerAura component.

Integration pattern:
```tsx
// In InterviewScreen
const [analyser, setAnalyser] = useState<AnalyserNode | null>(null);

// When starting audio recorder
const recorder = new AudioRecorder(onAudio);
await recorder.start();
setAnalyser(recorder.getAnalyser());

// Pass to visualizer
<AgentAudioVisualizerAura state={agentState} analyser={analyser} />
```
</context>

<next_action>
Start with: In InterviewScreen.tsx, capture the AnalyserNode from AudioRecorder.getAnalyser() and pass it to the AgentAudioVisualizerAura component. The hook will automatically start analyzing when the state is 'speaking' or 'listening'.
</next_action>