---
phase: 10-fix-gemini-live-transcription-quality-investigate-adk-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/geminiLiveClient.ts]
autonomous: false
requirements: [TRANSC-01, TRANSC-02, TRANSC-03]
user_setup: []

must_haves:
  truths:
    - "User speaks a sentence and sees it transcribed as complete words (not 'Bu ild a high tru st')"
    - "Transcription appears in real-time as user speaks"
    - "AI interviewer receives and responds to user speech correctly"
    - "Decision is documented explaining the approach chosen"
  artifacts:
    - path: "lib/geminiLiveClient.ts"
      provides: "Fixed WebSocket message format for audio streaming"
      contains: "realtimeInput: { media:"
    - path: ".planning/phases/10-fix-gemini-live-transcription-quality-investigate-adk-migration/10-DECISION.md"
      provides: "Documented decision on ADK migration"
  key_links:
    - from: "lib/geminiLiveClient.ts sendAudio()"
      to: "Gemini Live API"
      via: "WebSocket JSON message with realtimeInput.media format"
      pattern: "realtimeInput.*media.*mimeType"
---

<objective>
Fix the fragmented transcription issue in Gemini Live voice interviews by correcting the WebSocket message format in `sendAudio()`. Document the decision to keep the existing WebSocket implementation rather than migrating to ADK.

**Purpose:** User speech currently appears as fragmented characters ("Bu ild a high tru st") instead of complete words. This breaks the core interview UX.

**Output:** Fixed `sendAudio()` method with correct API message format, documented architecture decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-fix-gemini-live-transcription-quality-investigate-adk-migration/10-RESEARCH.md

<interfaces>
<!-- Key code structure from geminiLiveClient.ts -->

Current `sendAudio()` method (lines 209-223):
```typescript
sendAudio(base64Data: string) {
  if (this.ws?.readyState === WebSocket.OPEN) {
    const msg = {
      realtime_input: {
        media_chunks: [
          {
            data: base64Data,
            mime_type: "audio/pcm;rate=16000"
          }
        ]
      }
    };
    this.ws.send(JSON.stringify(msg));
  }
}
```

Correct format per ADK/SDK (from RESEARCH.md):
```typescript
const msg = {
  realtimeInput: {
    media: {
      data: base64Data,
      mimeType: "audio/pcm;rate=16000"
    }
  }
};
```

Note: The setup message in `connect()` uses snake_case correctly for v1beta API (`input_audio_transcription`, `generation_config`). Only the realtime audio message format needs fixing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix WebSocket message format in sendAudio()</name>
  <files>lib/geminiLiveClient.ts</files>
  <read_first>
    - lib/geminiLiveClient.ts (current sendAudio implementation)
  </read_first>
  <action>
    Fix the `sendAudio()` method to use correct Gemini Live API message format. Per RESEARCH.md, the current implementation uses incorrect snake_case field names and array structure.

    **Change the message format from:**
    ```typescript
    realtime_input: {
      media_chunks: [{ data, mime_type }]
    }
    ```

    **To:**
    ```typescript
    realtimeInput: {
      media: { data, mimeType }
    }
    ```

    Specific changes in `sendAudio()` method (lines 209-223):
    1. Change `realtime_input` to `realtimeInput` (camelCase)
    2. Change `media_chunks: [...]` to `media: {...}` (object, not array)
    3. Change `mime_type` to `mimeType` (camelCase)

    DO NOT modify the `connect()` method - its setup message correctly uses snake_case for v1beta API fields (`input_audio_transcription`, `generation_config`).
  </action>
  <acceptance_criteria>
    - `grep -n "realtimeInput:" lib/geminiLiveClient.ts` returns a match
    - `grep -n "media:" lib/geminiLiveClient.ts` returns a match inside sendAudio
    - `grep -n "mimeType:" lib/geminiLiveClient.ts` returns a match
    - `grep -n "realtime_input" lib/geminiLiveClient.ts` returns NO matches in sendAudio method
    - `grep -n "media_chunks" lib/geminiLiveClient.ts` returns NO matches
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The sendAudio() method sends correctly formatted WebSocket messages that the Gemini Live API recognizes for audio transcription.</done>
</task>

<task type="auto">
  <name>Task 2: Document ADK migration decision</name>
  <files>.planning/phases/10-fix-gemini-live-transcription-quality-investigate-adk-migration/10-DECISION.md</files>
  <read_first>
    - .planning/phases/10-fix-gemini-live-transcription-quality-investigate-adk-migration/10-RESEARCH.md
  </read_first>
  <action>
    Create a decision document recording the architecture choice for Phase 10.

    Document should include:
    1. **Problem:** Fragmented transcription caused by incorrect WebSocket message format
    2. **Options Evaluated:**
       - Option A: Fix existing WebSocket message format (CHOSEN)
       - Option B: Migrate to `@google/genai` SDK
       - Option C: Migrate to `@google/adk` (ADK)
    3. **Decision:** Fix existing WebSocket (Option A)
    4. **Rationale:**
       - ADK Web export does not include Live API session management
       - `@google/genai` SDK migration is optional - raw WebSocket works when format is correct
       - Fix is a simple 5-line change with LOW risk
       - No application restructuring required
    5. **Future Considerations:**
       - Consider `@google/genai` SDK migration for cleaner code
       - Consider ADK for multi-agent orchestration features
       - Consider AudioWorkletNode migration for deprecated ScriptProcessorNode

    Use the decision document format from GSD templates.
  </action>
  <acceptance_criteria>
    - File exists at `.planning/phases/10-fix-gemini-live-transcription-quality-investigate-adk-migration/10-DECISION.md`
    - Document contains "Decision:" section with "Fix existing WebSocket" or equivalent
    - Document contains "Rationale:" section explaining why ADK migration was not chosen
    - Document contains "Future Considerations:" section
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/10-fix-gemini-live-transcription-quality-investigate-adk-migration/10-DECISION.md && echo "Decision doc exists"</automated>
  </verify>
  <done>Architecture decision documented with clear rationale for keeping WebSocket implementation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify transcription quality in live interview</name>
  <what-built>Fixed WebSocket message format in geminiLiveClient.ts sendAudio() method. The message now uses correct camelCase field names (realtimeInput, media, mimeType) instead of snake_case.</what-built>
  <how-to-verify>
    **Manual testing required** - WebSocket and Live API integration cannot be unit tested.

    1. Run the dev server: `npm run dev`
    2. Start a voice interview with any agent
    3. Speak a complete sentence clearly (e.g., "Build a high trust, high standard culture")
    4. Verify the transcription shows complete words, NOT fragmented characters
    5. Verify AI interviewer responds correctly to your speech
    6. Test edge cases: fast speech, pauses, interruptions
  </how-to-verify>
  <resume-signal>Type "approved" if transcription works correctly, or describe the issue if fragmentation persists.</resume-signal>
</task>

</tasks>

<verification>
## Phase Verification

After all tasks complete:
1. Build passes: `npm run build`
2. Manual test confirms transcription quality
3. Decision document exists and is complete

## Requirement Coverage

| Requirement | Task | Verification |
|-------------|------|--------------|
| TRANSC-01 | Task 1 | Manual test - no character fragmentation |
| TRANSC-02 | Task 1 | Manual test - edge cases handled |
| TRANSC-03 | Task 2 | Decision document exists |
</verification>

<success_criteria>
1. User speech transcribes without character fragmentation (no "Bu ild a high tru st")
2. Audio streaming pipeline handles edge cases properly (silence, overlap, interruption)
3. Decision documented: keep current implementation with rationale
</success_criteria>

<output>
After completion, create `.planning/phases/10-fix-gemini-live-transcription-quality-investigate-adk-migration/10-01-SUMMARY.md`
</output>