---
phase: 07-candidate-persona-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/types.ts, lib/personaPrompts.ts, lib/personaExtractor.ts, lib/personaExtractor.test.ts]
autonomous: true
requirements: [CAND-01, CAND-02, CAND-03, CAND-04, CAND-05]
user_setup: []
must_haves:
  truths:
    - "System can extract skills, work history, and qualifications from resume text"
    - "System can infer experience level (junior/mid/senior/staff) from work history"
    - "System can detect communication style indicators from resume content"
    - "System can identify knowledge gaps by comparing resume to JD requirements"
    - "System generates coherent AI candidate persona with consistent traits"
  artifacts:
    - path: "lib/types.ts"
      provides: "CandidatePersona type definition"
      contains: "CandidatePersona"
    - path: "lib/personaPrompts.ts"
      provides: "Extraction prompt with JSON schema"
      exports: ["PERSONA_SCHEMA", "buildPersonaExtractionPrompt"]
    - path: "lib/personaExtractor.ts"
      provides: "Gemini extraction logic"
      exports: ["extractPersona", "validatePersonaOutput"]
  key_links:
    - from: "lib/personaExtractor.ts"
      to: "Gemini API"
      via: "GoogleGenerativeAI"
      pattern: "generateContent"
    - from: "lib/personaPrompts.ts"
      to: "lib/promptBuilder.ts"
      via: "MAX_CONTEXT_CHARS=8000, XML tags"
      pattern: "slice.*8000"
---

<objective>
Define CandidatePersona types and implement AI-powered persona extraction using Gemini.

Purpose: Enable coherent AI candidate persona generation from resume and job description data for Phase 8 simulation.
Output: Type definitions, extraction prompt, and extraction logic with validation helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-candidate-persona-generation/07-CONTEXT.md
@.planning/phases/07-candidate-persona-generation/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/types.ts:
```typescript
export type AgentId = 'hiring-manager' | 'high-pressure' | 'supportive-coach' | 'rapid-fire' | 'story-architect' | 'efficiency-screener' | 'behavioral-analyst';
```

From lib/debriefGenerator.ts (pattern to follow):
```typescript
const genAI = new GoogleGenerativeAI(apiKey);
const model = genAI.getGenerativeModel({ model: "gemini-2.0-flash" });
const result = await model.generateContent(prompt);
const text = result.response.text();
const jsonMatch = text.match(/\{[\s\S]*\}/);
const parsed = JSON.parse(jsonMatch[0]);
// Then validate...
```

From lib/promptBuilder.ts (pattern to follow):
```typescript
const MAX_CONTEXT_CHARS = 8000;
// XML-delimited context:
<resume>
${resume.slice(0, MAX_CONTEXT_CHARS)}
</resume>
```

From lib/agents.ts:
```typescript
// Pattern: Persona definitions with persona, tone, behaviors, boundaries
interface AgentDefinition {
  persona: string;
  coreBehaviors: string[];
  tone: string;
  boundaries: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CandidatePersona types to lib/types.ts</name>
  <files>lib/types.ts</files>
  <behavior>
    - CandidatePersona type includes experienceLevel (junior/mid/senior/staff)
    - CandidatePersona type includes skills with Technical/Soft/Domain categories
    - CandidatePersona type includes communicationStyle with formality and approach
    - CandidatePersona type includes knowledgeGaps array with category and missingSkills
    - Types are exported and usable by other modules
  </behavior>
  <action>
    Add the following types to lib/types.ts (append to end of file, before any existing exports):

    Per D-02, add:
    1. ExperienceLevel type: 'junior' | 'mid' | 'senior' | 'staff'
    2. CommunicationFormality type: 'formal' | 'casual'
    3. CommunicationApproach type: 'technical' | 'narrative'
    4. CommunicationStyle interface with formality and approach
    5. CategorizedSkills interface with technical, soft, domain arrays (string[])
    6. KnowledgeGap interface with category (string) and missingSkills (string[])
    7. CandidatePersona interface containing:
       - experienceLevel: ExperienceLevel
       - yearsOfExperience: number
       - workHistorySummary: string
       - skills: CategorizedSkills
       - communicationStyle: CommunicationStyle
       - knowledgeGaps: KnowledgeGap[] (per D-04: 3-5 categories)

    Follow existing type patterns in the file. Add JSDoc comments explaining each type.
  </action>
  <verify>
    <automated>npx tsc --noEmit lib/types.ts</automated>
  </verify>
  <done>Types compile without errors, CandidatePersona type exported and usable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create persona extraction prompts</name>
  <files>lib/personaPrompts.ts</files>
  <behavior>
    - PERSONA_SCHEMA constant defines JSON structure for AI output
    - buildPersonaExtractionPrompt accepts resume and jd strings
    - Prompt includes XML-delimited context following promptBuilder.ts pattern
    - Prompt includes experience level inference rules (junior: 0-2yr, mid: 2-5yr, senior: 5-10yr, staff: 10+yr)
    - Prompt includes skill categorization rules (technical/soft/domain)
    - Prompt includes communication style detection rules
    - Prompt includes knowledge gap analysis rules (3-5 categories, 2-3 skills each)
  </behavior>
  <action>
    Create lib/personaPrompts.ts following lib/analysisPrompts.ts patterns:

    1. Export PERSONA_SCHEMA constant - a JSON schema string describing the expected output format matching CandidatePersona type

    2. Export buildPersonaExtractionPrompt(resume: string, jd: string): string function that:
       - Uses MAX_CONTEXT_CHARS = 8000 (follow promptBuilder.ts)
       - Wraps resume and JD in XML tags: &lt;resume&gt; and &lt;jobDescription&gt;
       - Includes extraction rules for:
         - Experience Level: junior (0-2yr), mid (2-5yr), senior (5-10yr), staff (10+yr)
         - Skills: categorize as technical (languages/frameworks), soft (communication/leadership), domain (industry knowledge)
         - Communication Style: formality (formal/casual), approach (technical/narrative)
         - Knowledge Gaps: compare JD requirements to resume, output 3-5 categories with 2-3 specific missing skills each
       - Returns valid JSON matching PERSONA_SCHEMA

    Include explicit instructions in the prompt to prevent common pitfalls:
    - Year-based experience inference (not keyword matching)
    - Gap categories from JD requirements (not generic skills)
    - Structured output format (not narrative text)
  </action>
  <verify>
    <automated>npx vitest run lib/personaExtractor.test.ts -t "prompt"</automated>
  </verify>
  <done>Prompt module exports schema and builder function, follows existing patterns</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement persona extractor with validation</name>
  <files>lib/personaExtractor.ts, lib/personaExtractor.test.ts</files>
  <behavior>
    - extractPersona accepts resume and jd strings, returns Promise&lt;CandidatePersona&gt;
    - Uses Gemini API following debriefGenerator.ts pattern
    - Validates AI output with validatePersonaOutput helper
    - Returns fallback persona on API failure
    - Validation enforces experience level values, skill counts, gap structure
  </behavior>
  <action>
    Create lib/personaExtractor.ts following lib/debriefGenerator.ts patterns:

    1. Import GoogleGenerativeAI from @google/generative-ai
    2. Import CandidatePersona types from ./types
    3. Import buildPersonaExtractionPrompt from ./personaPrompts

    4. Export extractPersona(resume: string, jd: string): Promise&lt;CandidatePersona&gt;
       - Get API key from process.env.NEXT_PUBLIC_GEMINI_API_KEY
       - Use gemini-2.0-flash model
       - Call buildPersonaExtractionPrompt with truncated inputs
       - Parse JSON from response (use regex match pattern from debriefGenerator)
       - Validate with validatePersonaOutput
       - Return validated persona or throw error

    5. Export validatePersonaOutput(raw: unknown): CandidatePersona
       - Validate experienceLevel is one of the 4 valid values
       - Validate yearsOfExperience is a number (clamp to 0-50)
       - Validate workHistorySummary is a string
       - Validate skills has all 3 categories (fallback to empty arrays)
       - Validate communicationStyle has formality and approach
       - Validate knowledgeGaps is array with valid structure
       - Return typed CandidatePersona with fallbacks for missing fields

    Create lib/personaExtractor.test.ts with tests for:
    - validatePersonaOutput with valid input
    - validatePersonaOutput with missing fields
    - validatePersonaOutput with invalid enum values
    - buildPersonaExtractionPrompt includes XML tags
    - buildPersonaExtractionPrompt truncates long inputs
  </action>
  <verify>
    <automated>npx vitest run lib/personaExtractor.test.ts</automated>
  </verify>
  <done>All tests pass, extractor validates AI output, provides fallbacks</done>
</task>

</tasks>

<verification>
1. Types compile: `npx tsc --noEmit lib/types.ts`
2. Tests pass: `npx vitest run lib/personaExtractor.test.ts`
3. No import errors: `npx tsc --noEmit lib/personaExtractor.ts`
</verification>

<success_criteria>
1. CandidatePersona type defined with all required fields per D-02
2. Extraction prompt follows XML-delimited pattern from promptBuilder.ts
3. Extraction logic follows Gemini pattern from debriefGenerator.ts
4. Validation helpers enforce data quality with fallbacks
5. Unit tests cover validation edge cases
</success_criteria>

<output>
After completion, create `.planning/phases/07-candidate-persona-generation/07-01-SUMMARY.md`
</output>